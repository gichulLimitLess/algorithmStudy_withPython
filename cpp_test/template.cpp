#include <bits/stdc++.h>
using namespace std;

using ll = long long;
using pii = pair<int, int>;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
#ifdef LOCAL
    // 로컬 빌드에서만 같은 폴더의 input.txt를 표준입력으로 사용.
    // 제출 시에는 LOCAL이 정의되지 않으므로 이 블록은 통째로 사라진다.
    freopen("input.txt", "r", stdin);
#endif

    int n;
    cin >> n;
    cout << n << "\n";

    return 0;
}
