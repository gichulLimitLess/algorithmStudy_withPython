{
    "version": "2.0.0",
    "configurations": [
        {
            "name": "C++: 디버그 (CodeLLDB)",
            "type": "lldb",
            "request": "launch",
            "program": "${fileDirname}/.bin/${fileBasenameNoExtension}",
            "args": [],
            "cwd": "${fileDirname}",
            "preLaunchTask": "C++: 빌드 (디버그용)"
        }
    ]
}
